# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs

# Configure default editors to use.
export EDITOR=$(which vi)
export VISUAL=$(which nvim)

